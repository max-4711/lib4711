class Program
{
    static void Main(string[] args) //We're building a library, so nothing to do here - but as the project build type is a blazor app, we need this to statisfy the compiler
    {
    }
}